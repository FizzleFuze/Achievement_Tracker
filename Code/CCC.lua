-- See license.md for copyright info
local function Log(...)
    FFL_LogMessage(CurrentModDef.title, "CCC", ...)
end

function UpdateCCC()
    Log("BEGIN UpdateCCC()")
    local Menu, MenuEntry

end

function Test()
    --templates are not items :'(
    --make a button with a template of CommandCenterButton
    --for testing, make a new window on main game screen and call it on init


end